var sueldo = 20000;
if(sueldo> 2000){
    console.log("BIEN PAGADO");
    if(sueldo>10000){
        console.log("CATEGORIA A");
    }else{
        console.log("CATEGORIA B");
    }
}else{
    console.log("SIGUE CHAMBEANDO DURO");
}
// Operadores de Comparacion
// a==b => si a es igual que b
// a> b => si a es mayor que b
// a <b => si a es menor que b
// a>=b => si a es mayor o igual que b
// a<=b => si a es menor o igual que b
// a!=b => si a es diferente que b

// a===b => si a es igual que b Y a es el mismo tipo de variable que b
// a!==b => si a no es igual que b Y/O no son del mismo tipo de variables

// Operadores Logicos
// && => Y          o           AND
// || => O          o           OR
// !  => negacion   o           NOT

